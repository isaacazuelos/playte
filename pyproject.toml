[project]
name = "playte"
version = "0.1.0"
description = "A photo frame app for the Inkplate10 using MicroPython"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "esptool>=4.8",
    "mpremote>=1.24",
    "pillow>=10.0",
]
